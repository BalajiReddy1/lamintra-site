{
  "name": "lamintra-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 4174",
    "build": "astro build",
    "postbuild": "node scripts/sitemap.mjs",
    "preview": "astro preview --port 4175",
    "check": "astro check",
    "check:spacing": "node scripts/check-spacing.mjs",
    "check:links": "node scripts/check-links.mjs",
    "og-card": "node scripts/og-card.mjs",
    "optimise:images": "python scripts/optimise-images.py --write"
  },
  "devDependencies": {
    "astro": "^7.2.0"
  }
}
